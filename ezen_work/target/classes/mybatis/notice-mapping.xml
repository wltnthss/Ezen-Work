<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="mybatis.noticeMapper"> 		
 
 	<insert id="register" parameterType="noticeDTO">
 		insert into notice values (seq_notice.nextval, #{id}, #{subject}, #{content}, sysdate, 0)
 	</insert>
 	
 	<select id="List" parameterType="noticeDTO">
	<![CDATA[select seq, id, subject, content, to_char(day, 'YYYY.MM.DD') as day, count 
     from
	(select rownum rn, tt. * from
	(select * from notice order by seq desc) tt)
	where rn>=#{startNum} and rn<=#{Endnum}]]>\
 	</select>
 	
 	<select id="getTotal" resultType="int">
		select count(*) as cnt from notice
	</select>
	
	<update id="updateHit" parameterType="int">
		update notice set hit = hit + 1 where seq=#{seq}
	</update>
 </mapper>